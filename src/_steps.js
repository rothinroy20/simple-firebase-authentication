/**
 * Steps to use firebase
 * 1. Create a project on console.firebase.google.com
 * 2. install firebase
 * 3. Register web app in firebase
 * 4.copy firebase init with config from firebase project settings
 * 5. export default app
 * 6. import app firebase init.js into your app.js
 * 7. import getAuth from firebase/auth and creat auth = getAuth(app                                          )
 * 8. turn on google authentication (firebase >authentication> enable.googles sign)
 * 9. create google provider
 * 10. use singInWithPopup and pass auth and provider
 * 11. handle .then (if successful) and catch error (if error)
 * 
 */
